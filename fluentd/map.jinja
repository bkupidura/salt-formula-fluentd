{% set agent = salt['grains.filter_by']({
  'Debian': {
    'pkgs': ['td-agent'],
    'gems': ['fluent-plugin-prometheus', 'fluent-plugin-grok-parser'],
    'gem_path': 'td-agent-gem',
    'service_name': 'td-agent',
    'dir': {
      'config': '/etc/td-agent'
    },
  },
}, merge=salt['pillar.get']('fluentd:agent')) %}
